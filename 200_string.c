// gets() :- take string from user
// puts() :- display string
#include <stdio.h>
void main()
{
    char name[30];
    printf("enter name : ");
    gets(name);
    printf("name of student : ");
    puts(name);
}